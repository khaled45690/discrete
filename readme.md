## to test the Encryption algorithm

#### run server1 first and then on another terminal server 2

#### the next command lines

```
node server1.js
```

```
node server2.js
```

## Connect Four GUI

Play a GUI Connect Four game powered by an alpha-beta minimax AI.

run 

```
source /home/khaled/Documents/khaled/Programing/Projects/AI_development/colab_arabic_OCR/simpleocr_env/bin/activate
```
Install dependencies and run:

```
pip install -r requirements.txt
python connect4_gui.py
```

Options:

- `--depth N` set AI search depth (e.g. `python connect4_gui.py --depth 5`).

#### the write any text in server1 and press enter to be received in server two
